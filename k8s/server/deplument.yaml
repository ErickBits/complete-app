apiVersion: apps/v1
kind: Deployment
metadata:
  name: server
  labels:
    app: back-end
spec:
  replicas: 2
  selector:
    matchLabels:
      app: back-end
  template:
    metadata:
      labels:
        app: back-end
    spec:
      containers:
        - name: server
          image: 
          ports:
            - containerPort: 5100 
  